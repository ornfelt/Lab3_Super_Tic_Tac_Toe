using System;
using System.Collections.Generic;
using System.Text;

namespace Lab3
{
    class ValidateInput
    {
        // input values must be specific characters... Make ToLower() and check that they only contain n, c, w, e or ...
        public bool InputIsValid(String input)
        {
            return true;
        }
    }
}
